# @when("I modify the resource.")
# def modify_resource(resource):
#     resource.type = "Other"
#
#
# @when("The resource is synchronized.")
# def synchronize_resource(resource):
#     resource._synchronized = True


# @then("The synchronization status should be updated to False.")
# def check_synchronized(resource):
#     assert resource._synchronized == False
